int valor;
int b;

int soma (int, float);
float dim(char, string);

begin
    int valor;

    string testando;

    read testando;

    write "Informe um valor:";
    read valor;
    float g;
    while (valor > 4)
    begin
        write "\nvalor:";

        float testei;

        while(b > 7)
        begin

            int op;
            op = 4;
        end
    end
        read valor;
        


    if (b <= valor)
    begin
        write "Impressao interna ao IF!\n";
    end

    soma(4, g);

    dim("f", "dsa");
    
    
end

int soma(int a, float b)
begin
    write "Informe um valor:";

    float oi;

    dim("s", "ghg");
    
end

float dim(char oi, string g)
begin
    soma(4, 2.3);

    
end